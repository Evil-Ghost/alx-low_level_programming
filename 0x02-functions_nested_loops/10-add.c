#include "main.h"

/**
 * add - adds two integers and returns the
 * result
 * @a: first argument
 * @b: second argument
 *
 * Return: sum of two integers(sum)
 */
int add(int a, int b)
{
	int sum;

	sum = a + b;
	return (sum);
}
